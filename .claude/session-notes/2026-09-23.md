# 2026-09-23 — Plan, proposal, and Phase 1 built

## Did

- Enhanced the ChatGPT concept into a plan (ARTA instrument word for word, on-site vs online
  versions, ARTA scoring, anonymity vs linking, integrity routing to CART, stable QR URLs, phased
  build). User chose: Cloudflare Workers + D1; proposal first, then build; office collects CSM on
  paper and online today.
- Management proposal written as a Claude doc: <https://claude.ai/artifact/SwnJs7KwaTzRpXCGZBch4U>
  (includes the "what we need from each office" tracker).
- Built Phase 1: shared package (instrument, scoring, report, validation, importers), migration
  0001 with guard triggers, public Worker + survey UI, admin Worker + UI, backup script, docs.

## Verified

- 179 tests pass; typecheck and lint clean; both Workers build and dry-run deploy.
- Scoring reproduces a filed report's figures (PRA FY2025 CSM Report).
- Deliberately broke four guards and watched tests fail: scoring formula, role check, Access
  audience check, backup restore check (truncated export).
- End to end on local D1: submission via the dev server with live Siteverify (test keys), retry
  returned the same reference, the admin dashboard and CSV export saw it, formula text neutralized.

## Not verified

- The UIs in a browser (no browser automation available); real Access and Turnstile; phones.

## Repository (later the same day)

- Connected to `https://github.com/ancientsky14/mgb_feedback` (**public**). Local `main` was put on
  the remote's "Initial commit" (`095b779`, GitHub's placeholder README) without a force push; our
  README replaces it in the first real commit.
- Scrubbed for a public repo: no security details of other office systems, no personal notes;
  the roadmap now lives in `docs/roadmap.md`; `AGENTS.md` rule 0 says the repo is public.
- CI added: `.github/workflows/ci.yml` (Node 24, `npm ci`, lint, typecheck, tests).
- Lesson: `npm ci` deletes `node_modules` first. Check for running dev servers
  (`npm run dev:*`, `workerd.exe`) before running it, or it half-deletes the tree on Windows locks.

## Next

- The owner commits and pushes (`git add -A`, `git commit`, `git push -u origin main`), then
  checks the first CI run.
- Share the proposal; collect the Phase 0 inputs.
- Look at both UIs in a browser; fix what shows up.
- When the samples arrive: pin the importers, run the golden test.

## Watch

- The built-in form's PSA approval expired 31 July 2025 — get the current version.
- `office_overall_method` is unconfirmed (pooled vs mean of services).
